# 全局通用数据类型判断

p32

```js
function mygettype(obj) {
  let type = typeof obj;
  if (type !== "object" || type === null) return type;
  return Object.prototype.toString.call(obj).replace(/^\[object(\S+)\]$/, "$1");
}
```
