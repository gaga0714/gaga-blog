<PasswordProtect>

# dom
createElement新元素标签
createTextNode文本节点
createDocumentFragment一次性添加
createAttribute属性

querySelector 拿到首个 传任意css
querySelectorAll

parentNode childNodes firstChild lastChild nextSibling previousSibling

innerHTML
innerText/innerContent
style 遇到-转成驼峰

appendChild
setAttribute

# bom
浏览器对象原型
window
location
navigator
screen
history (go,forward,back,length)

# == ===
布尔转数值，字符串转数值
对象valueOf取原始值
undefined==null
有NaN就是false
都是对象比较是不是指向同一个对象

问题：
布尔和字符串怎么转
false=='false' f
false=='0' t
false==undefined f
false==null f
' \t\r\n'==0 0

# 判断类型
typeof(xxx)
typeof xxx
instanceof
Object.prototype.toString.call
```js
function myInstance(obj,function){
    if(typeof function!=='function') throw new TypeError();
    if(obj===null) return false;
    if(typeof obj!=='object') return false;
    let prototype=function.prototype;
    let proto=Object.getPrototypeOf(obj);
    while(proto!=null){
        if(proto===prototype){
            return true;
        }
        proto=Object.getPrototypeOf(proto);
    }
    return false;
}
```

# 原型链
每个函数都有一个特殊的属性叫做prototype
constructor指向构造函数
Object.prototype.__proto__===null
有Function.prototype.__proto__这种东西吗？

# 作用域链
全局作用域
函数作用域
块级作用域
词法作用域/静态作用域

链：js引擎在当前作用域找，没找到去上层作用域找

# this
默认绑定
隐式绑定

永远指向调用的！不是简单赋值，而是要调用

new绑定 new过程如果遇到return，this指向为返回的对象。简单类型的话还是实例对象

显式修改  call参数 apply数组 bind列表

箭头函数 不能作为构造函数

new > 显式 > 隐式 > 默认

```js
function mynew(Func,args){
    const obj={};
    obj.__proto__=Func.prototype;
    let result=Func.apply(obj,...args);
    return result instanceof Object ? result : obj;
}
```

定时器里调用的this会跑到全局执行上下文中去，this指向window

# 执行上下文

# 事件模型
捕获，目标，冒泡

dom0 绑定速度快，不支持捕获，同一类型事件只能绑定一次，删除置null
dom2
ie attachEvent,detachEvent

# 事件代理
冒泡阶段完成
增加删除的时候要特意的去给他们绑定和解绑
减少整个页面所需内存，动态绑定减少重复工作
focus,blur没有冒泡机制，mousemove,mouseout

# 闭包
创建私有变量，延长变量的生命周期

# 类型转换
Number(),parseInt(),String(),Boolean()

== 会触发 ToPrimitive
undefined,null,false,"",0,NaN

null转数值为0，undefined转数值为NaN

# 浅拷贝深拷贝
Object.assign,slice,concat, 拓展运算符

_.cloneDeep(),jQuery.extend(),JSON.stringify(),手写

```js
function DeepClone(obj,hash=new WeakMap()){
    if(obj==null) return obj;
    if(obj instanceof RegExp) return new RegExp(obj);
    if(obj instanceof Date) return new Date(obj);
    if(typeof obj!=='object') return obj;
    if(hash.has(obj)) return hash.get(obj);
    let deepObj=new obj.constructor();
    hash.set(obj,deepObj);
    for(let key in obj){
        if(obj.hasOwnProperty(key)){
            deepObj[key]=DeepClone(obj[key],hash);
        }
    }
    return deepObj;
}
```

# 函数缓存
用空间（缓存存储）换时间（计算过程）
闭包，柯里化，高阶函数

# 字符串
增+，concat，slice，substr，substring
改trim,repeat,padStart(几个，用什么（没有就默认是空格）),toLowerCase
查charAt,indexOf,startWith,includes
split拆成数组返回

match,search,replace

# 数组
增push,unshift,splice,concat
删pop,shift,splice,slice
查indexOf,includes,find返回第一个匹配的
排序reverse,sort
转换join
迭代
some有一个就返回true
every所有都true才true
forEach
filter是true的组成数组返回
map每一项都运行该函数

# 事件循环
实现单线程非阻塞的方法就是事件循环
微任务有Promise.then.catch.finally,mutationObserver,process.nextTick

宏任务有script,setTimeout,setInterval,requestAnimationFrame,postMessage,MessageChannel,setImmediate,I/O

async返回一个promise对象，await后面会加入微任务队列，

# js本地存储
cookie，sessionStorage，localStorage，indexedDB

localStorage 
5M，
设置setItem(),
获取getItem(),
获取键名key(0)拿到第一个键名，
删除removeItem()，
一次性清除所有存储clear()
无法设置过期时间，只能存字符串无法存对象

indexedDB，原声支持存储js对象，idb
https://github.com/chenstarx/GoDB.js
Godb.js降低操作难度

不管怎样简单的需求，在量级达到一定层次时，都会变得异常复杂。

# 大文件断点续传

服务器处理数据的能力，请求超时，网络波动

分片上传和断点续传

按照一定的分割规则，分割成相同大小的数据块；
初始化一个分片上传任务，返回本次分片上传唯一标识；
串行或并行发送各个分片数据块；
服务器判断是否上传完整，完整则合成得到原始文件

中断怎么办？
根据当前临时文件大小计算偏移量，从该位置继续读取
MD5是一种哈希函数，把任意长度的数据指纹化为128位的摘要，不可逆的
替代：文件消息完整性（SHA-256/512），消息认证（HMAC-SHA256），密码存储（Argon2id）

# ajax
全称 Async javascript and xml
XMLHttpRequest
open,send,inreadystatechange

# 防抖和节流

防抖是delay时间后执行，重复触发则重新计时
节流是delay时间内只执行一次

```js
function debounced(delay,fn){
    let timer=null;
    return funtion(...args){
        if(timer){
            clearTimeout(timer);
        }
        timer=setTimeout(()=>{
            fn.apply(this,args);
        },delay);
    }
}
```

```js
function throttle(fn,delay){
    let lasttime=0;
    return function(...args){
        let now=new Date().getTime();
        if(now-lasttime>=delay){
            fn.apply(this,args);
            lasttime=now;
        }
    }
}
```

# 元素是否在可视区域
offsetTop,scrollTop 只读的，每次都要重新访问
getBoundingClientRect
**Intersection Observer 不用进行事件的监听，性能好**
实践实践！！！！！！！

# 单点登录
sso（single sign on）
有一个认证中心，
访问系统1，发现未登录，跳转到认证中心，将自己的地址作为参数
认证中心发现用户没登录，
引导登录后，
创建用户和认证中心的全局会话，创建授权令牌
系统1拿到令牌去找认证中心问是不是有效，
有效就建系统1和用户的局部会话，

访问系统2，发现未登录，去找认证中心
认证中心说已经登录跳回系统2，带上令牌，
创建系统2和用户的局部会话

# 上拉加载，下拉刷新
可以用touch api
better-scroll库封装好了使用更方便

# 正则表达式
贪婪模式
懒惰模式

# 编程范式
命令式，声明式，函数式

纯函数就是无副作用的函数

# web常见的攻击方式
存储型xss 恶意代码在服务器（数据库/文件/缓存）中
反射型xss 恶意代码在url中
dom型xss 取出和执行恶意代码由浏览器端完成，属于前端js自身漏洞，其他是服务端安全漏洞

防止csrf，阻止不明外域访问，提交时要求附加本域才能获取的信息（双重cookie）
表单隐藏域 <input type="hidden" name="csrf">

sql注入：恶意 SQL 片段注入到应用的数据库查询里

https://github.com/febobo/nginx-tutorial

# 内存泄漏
垃圾回收机制
标记清除 进入环境，离开环境 两个状态
引用计数 引用次数是0就可以释放

内存泄漏
全局变量->this意外创建->使用严格模式
定时器
闭包
监听器
没有清理对dom元素的引用

# 继承
原型链继承
Child.prototype=new Parent()
解释是Child的所有实例都会有__proto__指向Parent的实例，Parent的实例指向Parent.prototype

构造函数继承

组合继承

原型式继承 object.create
寄生式继承 object.create

寄生组合式继承
extends

# 0.1+0.2!=0.3
0.1、0.2在二进制里是无限循环小数，只能近似表示
math.js bigdecimal.js
转换成整数

# 尾递归
```js
function fn(n,total){
    if(n===1) return total;
    return fn(n-1,total*n);
}
fn(5,1);
```
每次返回的是一个新的函数，不带上一个函数的参数，也就不需要储存上一个函数
只要保存一个调用栈，复杂度是O(1)

数组扁平化：
```js
function flat(arr=[],result=[]){
    arr.forEach(v=>{
        if(typeof v!=='object'){
            result.push(v);
        }else{
            result=result.concat(flat(v,[]));
            flat(v,result);
        }
    })
    return result;
}
```
数组对象格式化
斐波那契数列递归实现
阶乘


</PasswordProtect>