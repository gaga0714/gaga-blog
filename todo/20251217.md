```py

"""maas_lego 模块的单元测试"""import pytestimport numpy as npimport ifcopenshellfrom ifcopenshell.api.root import create_entityfrom maas_lego.core.project import (    create_empty_project,    create_single_building_project,    create_single_storey_building_project,)from maas_lego.core.spatial import (    create_site,    create_building,    create_storey,    create_space,)from maas_lego.core.structural import (    create_covering,    create_opening,)from maas_lego.core.style import add_element_colorfrom maas_lego.properties.pset import add_pset, edit_pset, remove_psetfrom maas_lego.relationships.rel import add_opening_to_element, add_filling_to_openingfrom maas_lego.constant.colors import DEFAULT_COLORSclass TestProject:    """测试项目创建功能"""    def test_create_empty_project(self):        """测试创建空项目"""        model = create_empty_project("测试项目")        assert model is not None        assert model.schema == "IFC4"                projects = model.by_type("IfcProject")        assert len(projects) == 1        assert projects[0].Name == "测试项目"                # 检查默认颜色是否已创建        materials = model.by_type("IfcMaterial")        assert len(materials) >= len(DEFAULT_COLORS)    def test_create_single_building_project(self):        """测试创建包含建筑的项目"""        model = create_single_building_project("建筑项目")        assert model is not None                sites = model.by_type("IfcSite")        assert len(sites) == 1                buildings = model.by_type("IfcBuilding")        assert len(buildings) == 1    def test_create_single_storey_building_project(self):        """测试创建包含楼层建筑的项目"""        model = create_single_storey_building_project("楼层项目")        assert model is not None                storeys = model.by_type("IfcBuildingStorey")        assert len(storeys) == 1        assert storeys[0].Elevation == 0.0class TestSpatial:    """测试空间元素创建功能"""    def test_create_site(self):        """测试创建站点"""        model = create_empty_project()        project = model.by_type("IfcProject")[0]                site = create_site(model, "测试站点", project)        assert site is not None        assert site.Name == "测试站点"        assert site.is_a() == "IfcSite"    def test_create_building(self):        """测试创建建筑"""        model = create_single_building_project()        site = model.by_type("IfcSite")[0]                building = create_building(model, "测试建筑", site)        assert building is not None        assert building.Name == "测试建筑"        assert building.is_a() == "IfcBuilding"    def test_create_storey(self):        """测试创建楼层"""        model = create_single_building_project()        building = model.by_type("IfcBuilding")[0]                storey = create_storey(model, "测试楼层", elevation=3.0, building=building)        assert storey is not None        assert storey.Name == "测试楼层"        assert storey.Elevation == 3.0        assert storey.is_a() == "IfcBuildingStorey"    def test_create_space(self):        """测试创建空间"""        model = create_single_storey_building_project()        storey = model.by_type("IfcBuildingStorey")[0]                space = create_space(            model,            name="测试空间",            placement=(1.0, 2.0, 3.0),            storey=storey,            long_name="长名称",            composition_type="ELEMENT",            predefined_type="INTERNAL",            elevation_with_flooring=0.1,        )        assert space is not None        assert space.Name == "测试空间"        assert space.LongName == "长名称"        assert space.CompositionType == "ELEMENT"        assert space.PredefinedType == "INTERNAL"        assert space.ElevationWithFlooring == 0.1    def test_create_space_with_psets(self):        """测试创建带属性集的空间"""        model = create_single_storey_building_project()        storey = model.by_type("IfcBuildingStorey")[0]                psets = {            "测试属性集": {                "面积": 100.0,                "类型": "办公室",            }        }                space = create_space(model, "带属性集的空间", storey=storey, psets=psets)        assert space is not None                # 验证属性集已创建        psets_found = [pset for pset in model.by_type("IfcPropertySet")                       if hasattr(pset, "Name") and pset.Name == "测试属性集"]        assert len(psets_found) > 0class TestStructural:    """测试结构元素创建功能"""    def test_create_covering(self):        """测试创建装饰面"""        model = create_single_storey_building_project()        storey = model.by_type("IfcBuildingStorey")[0]                covering = create_covering(            model,            name="测试地面",            placement=(0.0, 0.0, 0.0),            predefined_type="FLOORING",            storey=storey,        )        assert covering is not None        assert covering.Name == "测试地面"        assert covering.PredefinedType == "FLOORING"        assert covering.is_a() == "IfcCovering"    def test_create_opening(self):        """测试创建开口"""        model = create_single_storey_building_project()                opening = create_opening(            model,            name="测试开口",            predefined_type="OPENING",            placement=(0.0, 0.0, 0.0),        )        assert opening is not None        assert opening.Name == "测试开口"        assert opening.PredefinedType == "OPENING"        assert opening.is_a() == "IfcOpeningElement"class TestStyle:    """测试样式和颜色功能"""    def test_add_element_color(self):        """测试添加元素颜色"""        model = create_empty_project()        body = [c for c in model.by_type("IfcGeometricRepresentationContext")                 if c.ContextIdentifier == 'Body'][0]                rgb = {"Red": 1.0, "Green": 0.5, "Blue": 0.0}        material = add_element_color(            model,            name="测试颜色",            rgb=rgb,            transparency=0.2,            body_context=body,        )        assert material is not None        assert material.Name == "测试颜色"    def test_add_element_color_default_rgb(self):        """测试添加元素颜色（使用默认RGB）"""        model = create_empty_project()        body = [c for c in model.by_type("IfcGeometricRepresentationContext")                 if c.ContextIdentifier == 'Body'][0]                material = add_element_color(model, name="默认颜色", body_context=body)        assert material is not Noneclass TestPset:    """测试属性集功能"""    def test_add_pset(self):        """测试添加属性集"""        model = create_single_storey_building_project()        space = create_space(model, "测试空间")                properties = {            "面积": 50.0,            "类型": "办公室",            "编号": "A101",        }                pset = add_pset(model, space, name="测试属性集", properties=properties)        assert pset is not None        assert pset.Name == "测试属性集"    def test_add_pset_default_name(self):        """测试添加属性集（使用默认名称）"""        model = create_single_storey_building_project()        space = create_space(model, "测试空间")                pset = add_pset(model, space, properties={"测试": "值"})        assert pset is not None        assert "Space" in pset.Name    def test_edit_pset(self):        """测试编辑属性集"""        model = create_single_storey_building_project()        space = create_space(model, "测试空间")                pset = add_pset(model, space, name="测试属性集", properties={"初始": "值"})                new_properties = {"更新": "新值", "另一个": 123}        edit_pset(model, pset, properties=new_properties)                # 验证属性已更新        assert pset is not None    def test_remove_pset(self):        """测试删除属性集"""        model = create_single_storey_building_project()        space = create_space(model, "测试空间")                pset = add_pset(model, space, name="待删除属性集")                remove_pset(model, space, pset)                # 验证属性集已删除        psets = [p for p in model.by_type("IfcPropertySet")                 if hasattr(p, "Name") and p.Name == "待删除属性集"]        assert len(psets) == 0class TestRelationships:    """测试关系功能"""    def test_add_opening_to_element(self):        """测试将开口添加到元素"""        model = create_single_storey_building_project()                # 创建一个墙元素        wall = create_entity(model, ifc_class="IfcWall", name="测试墙")                # 创建开口        opening = create_opening(model, name="测试开口")                # 将开口添加到墙        add_opening_to_element(model, opening=opening, element=wall)                # 验证关系已创建        voids_rels = model.by_type("IfcRelVoidsElement")        assert len(voids_rels) > 0    def test_add_filling_to_opening(self):        """测试将填充元素添加到开口"""        model = create_single_storey_building_project()                # 创建开口        opening = create_opening(model, name="测试开口")                # 创建门作为填充元素        door = create_entity(model, ifc_class="IfcDoor", name="测试门")                # 将门添加到开口        add_filling_to_opening(model, opening=opening, element=door)                # 验证关系已创建        fills_rels = model.by_type("IfcRelFillsElement")        assert len(fills_rels) > 0class TestConstants:    """测试常量"""    def test_default_colors(self):        """测试默认颜色常量"""        assert len(DEFAULT_COLORS) > 0                # 验证每个颜色都有名称、RGB和透明度        for name, rgb, transparency in DEFAULT_COLORS:            assert isinstance(name, str)            assert isinstance(rgb, dict)            assert "Red" in rgb            assert "Green" in rgb            assert "Blue" in rgb            assert isinstance(transparency, (int, float))

```

已创建 maas_lego 模块的单元测试文件 tests/test_maas_lego.py。测试覆盖：
项目创建：空项目、单建筑项目、单楼层项目
空间元素：站点、建筑、楼层、空间（含属性集）
结构元素：装饰面、开口
样式和颜色：添加元素颜色
属性集：添加、编辑、删除
关系：开口与元素、填充与开口
常量：默认颜色
运行测试：
pytest tests/test_maas_lego.py -v
如需添加更多测试用例或调整，请告知。