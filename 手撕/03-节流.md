```js
function throttle(fn,delay){
    let lasttime=0;
    return function(...args){
        let nowtime=new Date.getTime();
        if(nowtime-lasttime>=delay){
            fn.apply(this,args);
            lasttime=nowtime;
        }
    }
}
```